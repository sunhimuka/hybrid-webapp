name: Auto Commit from Chat

on:
  workflow_dispatch:

# GitHub Actions に push 権限を付与
permissions:
  contents: write

jobs:
  generate-and-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Generate code from ChatGPT
        run: |
          echo "ここにChatGPT経由で生成するスクリプトを配置"
          # 例: echo "console.log('Hello from ChatGPT!');" > src/generated.js

      - name: Install dependencies
        run: npm install

      - name: Commit changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Auto Commit from Chat 🚀" || echo "No changes to commit"
          git push origin main

  deploy:
    needs: generate-and-commit
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Vercel
        run: |
          npm install -g vercel
          vercel --token=${{ secrets.VERCEL_TOKEN }} --prod --confirm \
            --scope ${{ secrets.VERCEL_ORG_ID }} \
            --project ${{ secrets.VERCEL_PROJECT_ID }}

  notify:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Send notification email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.GMAIL_USERNAME }}
          password: ${{ secrets.GMAIL_APP_PASSWORD }}
          subject: "Hybrid WebApp - GitHub Actions通知"
          to: ${{ secrets.GMAIL_USERNAME }}
          from: GitHub Actions <${{ secrets.GMAIL_USERNAME }}>
          body: |
            ✅ Commit & Deploy 完了しました！

            - Repository: ${{ github.repository }}
            - Workflow: ${{ github.workflow }}
            - Run ID: ${{ github.run_id }}
            - Status: success
