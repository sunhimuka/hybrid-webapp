name: Auto Commit from Chat

on:
  workflow_dispatch:

permissions:
  contents: write   # ← これを追加

jobs:
  commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: true
          fetch-depth: 0

     - name: Make changes
  run: |
    mkdir -p pages

    # contact.js
    cat << 'EOF' > pages/contact.js
    export default function Contact() {
      return (
        <div style={{ padding: '2rem' }}>
          <h1>Contact Page 📞</h1>
          <p>This is a test page generated automatically!</p>
        </div>
      );
    }
    EOF

    # about.js
    cat << 'EOF' > pages/about.js
    export default function About() {
      return (
        <div style={{ padding: '2rem' }}>
          <h1>About Page ℹ️</h1>
          <p>This is another page generated automatically!</p>
        </div>
      );
    }
    EOF


    # about.js
    cat << 'EOF' > pages/about.js
    export default function About() {
      return (
        <div style={{ padding: '2rem' }}>
          <h1>About Page ℹ️</h1>
          <p>This is another page generated automatically!</p>
        </div>
      );
    }
    EOF


      - name: Commit changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto Commit from Chat: Add contact.js"
            git push
          fi
